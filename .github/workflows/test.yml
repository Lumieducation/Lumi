name: Build + Tests

on:
    push:
        branches:
            - master
    pull_request:

jobs:
    build_test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Use Node.js 14.x
              uses: actions/setup-node@v1
              with:
                  node-version: 14.x
            - name: NPM Setup
              run: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
              env:
                  NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
            - name: install
              uses: ianwalter/puppeteer-container@afefc4bee7341a77baa94503da684e2895ea5d30  
              with:
                  args: npm ci && (cd client && npm ci) && (cd reporter-client && npm ci)
            - name: lint
              uses: ianwalter/puppeteer-container@afefc4bee7341a77baa94503da684e2895ea5d30  
              with:
                  args: npm run lint
            - name: build
              uses: ianwalter/puppeteer-container@afefc4bee7341a77baa94503da684e2895ea5d30  
              with:
                  args: npm run build:server
            - name: build:client
              uses: ianwalter/puppeteer-container@afefc4bee7341a77baa94503da684e2895ea5d30  
              with:
                  args: npm run build:client
            - name: build:reporter-client
              uses: ianwalter/puppeteer-container@afefc4bee7341a77baa94503da684e2895ea5d30  
              with:
                  args: npm run build:reporter-client
            - name: unit-tests
              uses: ianwalter/puppeteer-container@afefc4bee7341a77baa94503da684e2895ea5d30  
              with:
                  args: npm run test
              env:
                  CI: true
            - name: e2e-test
              uses: ianwalter/puppeteer-container@afefc4bee7341a77baa94503da684e2895ea5d30
              with:
                  args: npm run test:e2e
              env:
                  CI: true    